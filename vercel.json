{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install --ignore-scripts --frozen-lockfile=false",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.js": {
      "runtime": "nodejs22.x"
    }
  },
  "build": {
    "env": {
      "SKIP_ENV_VALIDATION": "1"
    }
  }
}
